import ParkTable from "./components/ParkTable/ParkTable";

// Placeholder — replaced by the real server-paged facilities table in §5.
export default function FacilitiesTablePanel() {
  return <ParkTable />;
}
