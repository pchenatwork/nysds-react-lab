import ParkTable from "./components/ParkTable/ParkTable";

// Tab 1 — server-paged, sortable, searchable table demo (NYS parks data).
export default function PaginatedTableDemoPanel() {
  return <ParkTable />;
}
